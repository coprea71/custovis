# Plan 12: Veröffentlichung

Gemeinsame Grundlage: [0.md](0.md). Voraussetzung: alle vorherigen Pläne.

## Ziel dieser Session

Web-Installer final, CI, Demo-Daten, erstes Release.

## Aufgaben

- Web-Installer (`/install`, siehe [0.md](0.md) Repo-Struktur) final
  ausbauen: DB-Test, Migrationen, Admin-Anlage, Abschluss-Sperre
  (`storage/installed.lock`).
- Schema-Update-Route `/admin/system/migrate` (Permission
  `system.maintain`) für spätere Versionen ohne Shell-Zugriff.
- GitHub-Actions-CI: Tests (`php artisan test`), Lint/Static Analysis.
- Demo-Seeder für ein realistisches Vorführ-Setup (Teams, Tickets aller
  Typen, KB-Artikel, Chat-Nachrichten).
- Finaler README-/CHANGELOG-Check vor Release-Tag.
- Erstes GitHub-Release (Tag, Release Notes aus CHANGELOG).

## Verifikation

- Frischer Web-Installer-Durchlauf auf einer leeren DB (nur SFTP/FTP-
  Upload, kein Shell-Zugriff simuliert) führt zu einer funktionsfähigen
  Instanz.
- CI-Pipeline läuft grün auf einem frischen Branch.
- Demo-Seeder erzeugt ein konsistentes, vorführbares Test-System.

## Stand 2026-09-24 (abarbeiten)

Umgesetzt (Commit `2145b9b`):
- Web-Installer `/install` (ohne Session/APP_KEY lauffähig, gesperrt per
  `storage/installed.lock` bzw. automatisch bei vorhandenen Benutzern),
  `php artisan custovis:install`, Schema-Updates unter `/admin/system/migrate`.
- `DemoSeeder` (Teams, Agenten, Kunden, Tickets aller ITIL-Typen, KB, Chat,
  Außendienst-Termin); Frischinstallation inkl. Demo-Daten gegen leere
  MySQL-DB lokal erfolgreich verifiziert.
- GitHub-Actions: `ci.yml` (Tests PHP 8.2–8.4, Pint, composer audit/validate),
  `release.yml` (upload-fertiges ZIP + Release-Notes aus dem CHANGELOG bei Tag `v*`).
- README/CHANGELOG final geprüft, CHANGELOG als `0.1.0` abgeschlossen,
  composer.json-Metadaten (Name, AGPL-Lizenz) korrigiert.

## Stand 2026-09-25

Erledigt:
- Öffentliches Repository https://github.com/coprea71/custovis (AGPLv3) angelegt,
  lokaler `master` trackt `origin/main`; CI läuft auf GitHub grün.
- Issue-Vorlage für Fehlermeldungen und vertraulicher Sicherheitskanal
  (GitHub Private Vulnerability Reporting, verlinkt in `SECURITY.md`).

Offen – braucht eine Entscheidung/Aktion des Nutzers (nach außen wirkend):
- In den Repo-Settings *Private vulnerability reporting* aktivieren (Voraussetzung
  für den Meldelink in `SECURITY.md`).
- Release veröffentlichen: `git tag -a v0.1.0 -m "Custovis 0.1.0"` und
  `git push origin v0.1.0` – der Release-Workflow erstellt daraus das
  GitHub-Release mit ZIP-Archiv und Release-Notes.
